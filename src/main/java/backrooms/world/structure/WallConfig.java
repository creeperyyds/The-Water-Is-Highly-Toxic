package backrooms.world.structure;

import com.mojang.serialization.Codec;
import com.mojang.serialization.codecs.RecordCodecBuilder;
import net.minecraft.world.gen.blockstateprovider.BlockStateProvider;
import net.minecraft.world.gen.feature.IFeatureConfig;
/*
public class WallConfig implements IFeatureConfig {
    public static final Codec<WallConfig> CODEC = RecordCodecBuilder.create(k1 -> {
        return k1.group(BlockStateProvider.CODEC.fieldOf("type").orElseGet(() -> {

        }))
    })
}

 */
